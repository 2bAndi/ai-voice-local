# Radicale configuration (local CalDAV server, legacy appointment demo)
# Start:  .\start_radicale.ps1   (sets the working directory to <project>\radicale-data)
[server]
hosts = 127.0.0.1:5232

[auth]
# reachable locally only, therefore without authentication
type = none

[storage]
filesystem_folder = ./collections
